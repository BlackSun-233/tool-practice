## Issue
标题：传入全空白姓名仍输出问候语
环境：待确认
复现命令：`sdt‑greet --name "   "`
实际结果：程序输出 `Hello, !`，退出码为0
期望结果：识别全空白姓名，以非0状态码退出

## Git提交信息
修复全空白姓名仍然输出问候的缺陷
问题：传入全部空白字符的name参数，程序依旧打印问候，返回码0；
方案：增加空白字符判断，检测到全空白输入时调用sys.exit(2)退出。

## 评审意见
- 行为：未校验name参数全空白场景，直接执行输出；
- 风险：空名字输出无效问候，调用方无法识别异常输入；
- 建议：增加输入校验，全空白输入返回码2阻断执行。【Blocking】
